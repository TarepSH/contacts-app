<div class="text-center">
<%= bootstrap_form_for([@user, @user.contacts.build], layout: :horizontal, label_col: "col-sm-4", control_col: "col-sm-4", class: "center-block") do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.text_field :company %>
  </div>

  <div class="field">
    <%= f.text_area :address %>
  </div>

  <div class="field">
    <%= f.number_field :phone %>
  </div>

  <div class="field">
    <%= f.date_field :birthday %>
  </div>

 
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>